
<div class="section">
    <div class="title">{{ config('app.name') }}</div>
    <div>{{ db_trans('receipts.title') }}</div>
    <div>{{ db_trans('receipts.number') }}: {{ $receipt->receipt_no }}</div>
</div>
