
<div class="section">
    <strong>{{ db_trans('receipts.transaction_details') }}</strong><br>
    {{ db_trans('receipts.amount') }}: {{ $receipt->amount }}<br>
    {{ db_trans('receipts.type') }}: {{ $receipt->type }}<br>
    {{ db_trans('receipts.date') }}: {{ $receipt->date }}<br>
</div>
