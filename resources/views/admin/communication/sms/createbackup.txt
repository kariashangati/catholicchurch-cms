@extends('layouts.admin')

@section('content')

<div class="container-fluid">

    <!-- HERO HEADER -->
    <div class="cc-hero mb-4">
        <div>
            <h2 class="cc-title">📩 {{ db_trans('communication.send_sms') }}</h2>
            <p>{{ db_trans('communication.send_sms_desc') }}</p>
        </div>
    </div>

    <form method="POST" action="{{ route('admin.communication.sms.store') }}">
        @csrf

        <div class="row">

            <!-- LEFT -->
            <div class="col-md-8">
                <div class="cc-card">

                    <label>{{ db_trans('communication.message') }}</label>
                    <textarea name="message" class="form-control" rows="5" required></textarea>

                    <div class="mt-3">
                        <label>{{ db_trans('communication.audience') }}</label>

                        <select name="audience_type" class="form-control">
                            <option value="all_members">All Members</option>
                            <option value="kanda">By Kanda</option>
                            <option value="jumuiya">By Jumuiya</option>
                            <option value="familia">By Familia</option>
                        </select>
                    </div>

                </div>
            </div>

            <!-- RIGHT -->
            <div class="col-md-4">
                <div class="cc-card">

                    <h5>📊 Preview</h5>

                    <div id="previewBox">
                        <p>Recipients: 0</p>
                        <p>Segments: 0</p>
                        <p>Total Cost: 0</p>
                    </div>

                    <button type="button" id="previewBtn" class="btn btn-primary w-100 mt-3">
                        Preview
                    </button>

                    <button class="btn btn-success w-100 mt-2">
                        Send SMS
                    </button>

                </div>
            </div>

        </div>

    </form>

</div>

@endsection