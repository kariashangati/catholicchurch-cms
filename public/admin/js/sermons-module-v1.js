(function(){'use strict';function initTables(){if(window.jQuery&&jQuery.fn.DataTable){['#sermonsTable','#sermonRequestsTable','#sermonRecipientsTable'].forEach(function(id){if(jQuery(id).length&&!jQuery.fn.DataTable.isDataTable(id)){jQuery(id).DataTable({pageLength:10,order:[],language:{search:'Tafuta:',lengthMenu:'Onyesha _MENU_ rekodi',info:'Inaonyesha _START_ - _END_ / _TOTAL_',zeroRecords:'Hakuna rekodi',paginate:{previous:'Iliyotangulia',next:'Inayofuata'}}});}});}}function initCharts(){if(typeof Chart==='undefined'||!window.sermonDashboardData){return;}var monthly=document.getElementById('sermonMonthlyChart');if(monthly){new Chart(monthly,{type:'line',data:{labels:window.sermonDashboardData.monthly.map(function(i){return i.label;}),datasets:[{label:'Mahubiri',data:window.sermonDashboardData.monthly.map(function(i){return i.value;}),borderColor:'#7c3aed',backgroundColor:'rgba(124,58,237,.14)',fill:true,tension:.35,borderWidth:3}]},options:{responsive:true,maintainAspectRatio:false,scales:{y:{beginAtZero:true,ticks:{precision:0}}}}});}var status=document.getElementById('sermonRequestStatusChart');if(status){new Chart(status,{type:'doughnut',data:{labels:window.sermonDashboardData.requestStatus.map(function(i){return i.label;}),datasets:[{data:window.sermonDashboardData.requestStatus.map(function(i){return i.value;}),backgroundColor:['#7c3aed','#0ea5e9','#10b981','#64748b','#ef4444']}]},options:{responsive:true,maintainAspectRatio:false,plugins:{legend:{position:'bottom'}}}});}}function initTiny(){if(window.tinymce){tinymce.init({selector:'.js-tinymce',height:420,menubar:false,plugins:'lists link table code image',toolbar:'undo redo | blocks | bold italic underline | alignleft aligncenter alignright | bullist numlist | link table image | code'});}}function initVideoSource(){document.querySelectorAll('.sermon-video-source').forEach(function(select){function sync(){var form=select.closest('form')||document;form.querySelectorAll('.sermon-video-upload').forEach(function(el){el.style.display=select.value==='upload'?'':'none';});form.querySelectorAll('.sermon-video-url').forEach(function(el){el.style.display=select.value==='url'?'':'none';});}select.addEventListener('change',sync);sync();});}function initTargets(){document.querySelectorAll('.sermon-target-type').forEach(function(select){var form=select.closest('form')||document;var checklist=form.querySelector('[data-sermon-target-checklist]');var search=form.querySelector('.sermon-target-search');var counter=form.querySelector('[data-sermon-target-counter]');var selectedWrap=form.querySelector('[data-sermon-target-selected]');var empty=form.querySelector('[data-sermon-target-empty]');var lastType=select.value;function checks(){return checklist?Array.prototype.slice.call(checklist.querySelectorAll('.sermon-target-check')):[];}function groups(){return checklist?Array.prototype.slice.call(checklist.querySelectorAll('.sermon-target-check-group')):[];}function checkedItems(){return checks().filter(function(input){return input.checked&&!input.disabled;});}function itemLabel(input){var item=input.closest('.sermon-target-check-item');var text=item?item.querySelector('.sermon-target-check-text'):null;return text?text.textContent.replace(/\s+/g,' ').trim():'';}function updateSummary(){var type=select.value;var selected=checkedItems();if(counter){counter.textContent=type==='all'?'ALL':String(selected.length);}if(selectedWrap){selectedWrap.innerHTML='';if(type!=='all'&&selected.length){selected.slice(0,10).forEach(function(input){var badge=document.createElement('span');badge.className='sermon-selected-chip';badge.textContent=itemLabel(input);selectedWrap.appendChild(badge);});if(selected.length>10){var more=document.createElement('span');more.className='sermon-selected-chip more';more.textContent='+'+(selected.length-10);selectedWrap.appendChild(more);}}}if(empty){empty.classList.toggle('d-none',type!=='all');}}function applySearch(){if(!checklist){return;}var type=select.value;var term=(search?search.value:'').toLowerCase().trim();groups().forEach(function(group){var matchGroup=type!=='all'&&group.getAttribute('data-type')===type;group.hidden=!matchGroup;group.classList.toggle('d-none',!matchGroup);group.querySelectorAll('.sermon-target-check-item').forEach(function(item){var input=item.querySelector('.sermon-target-check');var text=(item.getAttribute('data-search-text')||item.textContent||'').toLowerCase();var show=matchGroup&&(!term||text.indexOf(term)!==-1);item.hidden=!show;item.classList.toggle('d-none',!show);if(input){input.disabled=!show;if(!show){input.checked=false;item.classList.remove('is-checked');}}});});updateSummary();}function syncCheckedClasses(){checks().forEach(function(input){var item=input.closest('.sermon-target-check-item');if(item){item.classList.toggle('is-checked',input.checked&&!input.disabled);}});}function clearAll(){checks().forEach(function(input){input.checked=false;input.disabled=true;var item=input.closest('.sermon-target-check-item');if(item){item.classList.remove('is-checked');}});}function sync(){var type=select.value;form.querySelectorAll('[data-target-hint]').forEach(function(el){el.classList.toggle('active',el.getAttribute('data-target-hint')===type);});if(type==='all'){clearAll();groups().forEach(function(group){group.hidden=true;group.classList.add('d-none');});if(search){search.value='';search.setAttribute('disabled','disabled');}}else{if(search){search.removeAttribute('disabled');}if(lastType!==type){checks().forEach(function(input){input.checked=false;});}}lastType=type;applySearch();syncCheckedClasses();updateSummary();}if(search){search.addEventListener('input',applySearch);}if(checklist){checks().forEach(function(input){input.addEventListener('change',function(){syncCheckedClasses();updateSummary();});});}select.addEventListener('change',sync);sync();});}function initConfirm(){document.querySelectorAll('[data-sermon-confirm-delete]').forEach(function(form){form.addEventListener('submit',function(e){if(window.Swal){e.preventDefault();Swal.fire({icon:'warning',title:'Una uhakika?',text:'Hatua hii haiwezi kurejeshwa kirahisi.',showCancelButton:true,confirmButtonText:'Ndiyo',cancelButtonText:'Hapana',confirmButtonColor:'#dc2626'}).then(function(r){if(r.isConfirmed){form.submit();}});}else if(!confirm('Una uhakika?')){e.preventDefault();}});});}document.addEventListener('DOMContentLoaded',function(){initTables();initCharts();initTiny();initVideoSource();initTargets();initConfirm();});})();
